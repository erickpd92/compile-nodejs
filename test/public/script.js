// React Component to render "Hello World!"
function App() {
    return (
        <div>
            <h1>Hello World!</h1>
        </div>
    );
}

// Rendering the App component to the root div
ReactDOM.render(<App />, document.getElementById('root'));
